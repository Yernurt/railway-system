# 🚆 Railway Wagon Tracking System

Данный проект представляет собой **автоматизированную систему распознавания номеров железнодорожных вагонов** с последующей **передачей данных через RabbitMQ в backend**, сохранением информации в базе данных и **отслеживанием текущей станции и времени прибытия вагона**.

Система полностью работает в **Docker-контейнерах** и запускается через **docker-compose**.

---

## 🎯 Назначение системы

Проект предназначен для автоматизации учёта и мониторинга железнодорожных вагонов.

Система позволяет:

* автоматически **распознавать номер вагона** с изображения или видеопотока;
* фиксировать **время распознавания (прибытия)**;
* передавать данные (**номер вагона + время**) в backend **через RabbitMQ**;
* **сохранять данные в базу**;
* определять, **на какую станцию прибыл вагон**;
* просматривать информацию через **веб-интерфейс**.

---

## 🧩 Структура проекта

```text
railway-system/
├── Zernovoz-1/          # Detector (AI, Python + YOLO)
├── rail_v2/             # Backend (Java Spring Boot)
├── frontend/            # Frontend (React)
├── docker-compose.yml   # Docker Compose (infra)
├── .gitignore
└── README.md
```

---

## 🧠 Компоненты системы

### 🔹 Detector (Zernovoz-1)

AI-сервис на Python с использованием **YOLO**:

* анализирует изображение или видеокадр вагона;
* распознаёт **номер вагона**;
* формирует сообщение с **номером вагона и временем**;
* отправляет данные в **RabbitMQ**.

---

### 🔹 RabbitMQ

Используется как **очередь сообщений** между Detector и Backend.

Назначение:

* асинхронная передача данных;
* надёжная доставка сообщений;
* отсутствие прямой зависимости между AI и backend.

---

### 🔹 Backend (rail_v2)

Серверная часть системы на **Java Spring Boot**:

* получает сообщения из **RabbitMQ**;
* обрабатывает номер вагона и время;
* определяет **станцию прибытия**;
* сохраняет данные в **базу данных**;
* предоставляет API для Frontend.

---

### 🔹 Frontend

Веб-интерфейс на **React**:

* отображает список вагонов;
* показывает текущую станцию;
* отображает **время прибытия вагона**;
* взаимодействует с Backend через HTTP API.

---

## 🔄 Как работает система

1. Камера или источник данных передаёт изображение с вагоном.
2. **Detector (AI)**:

   * распознаёт номер вагона с помощью YOLO;
   * фиксирует время;
   * отправляет сообщение в **RabbitMQ**.
3. **Backend**:

   * читает сообщение из RabbitMQ;
   * сохраняет номер вагона, станцию и время в базу данных.
4. **Frontend**:

   * запрашивает данные у Backend;
   * отображает информацию пользователю.

Все сервисы взаимодействуют между собой внутри Docker-сети.

---

## ⚙️ Требования

Для запуска проекта необходимо:

* **Docker**
* **Docker Compose**

Проверка:

```bash
docker --version
docker compose version
```

---

## ▶️ Запуск проекта

```bash
git clone https://github.com/YOUR_USERNAME/railway-system.git
cd railway-system
docker compose up --build
```

После запуска все сервисы будут автоматически подняты.

---

## 🌐 Доступ к сервисам

| Сервис   | Адрес                                            |
| -------- | ------------------------------------------------ |
| Frontend | [http://localhost:3000](http://localhost:3000)   |
| Backend  | [http://localhost:8080](http://localhost:8080)   |
| RabbitMQ | [http://localhost:15672](http://localhost:15672) |
| Detector | Внутренний сервис (Docker network)               |

> Порты могут отличаться в зависимости от конфигурации `docker-compose.yml`.

---

## 🐳 Docker и контейнеризация

Каждый компонент системы запускается в собственном контейнере и управляется через `docker-compose.yml`.

---

## 🧹 Git и файлы проекта

В репозитории **не хранятся**:

* `node_modules`
* `.venv`
* IDE-файлы (`.idea`, `.vscode`)
* временные и runtime-файлы

Они исключены с помощью `.gitignore`.

---

## 🛠️ Полезные команды

Остановить систему:

```bash
docker compose down
```

Пересобрать и запустить заново:

```bash
docker compose up --build
```

---

## 📄 Лицензия

Проект разработан для **личного и практического использования**.
